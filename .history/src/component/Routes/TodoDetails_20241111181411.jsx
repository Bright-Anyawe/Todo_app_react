
import {
  Dialog,
  DialogTitle,
  DialogContent,
  DialogActions,
  Button,
  Typography,
} from "@mui/material";

export const TodoDetails = ({ open, onClose, todo }) => {
     
  if (!todo) return null;


    return <>
    </>;
}