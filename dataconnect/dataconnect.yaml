specVersion: "v1beta"
serviceId: "todoappreact"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todoappreact-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
